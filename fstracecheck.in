#!/usr/bin/env bash

flags=(
    --cc=@CC@
    -Iinclude
)

libconfig_flags=$(@CONFIG_PARSER@ |
    tr ' ' '\n' |
    grep -v '^-[lL]')

exec @FSTRACECHECK@ @CPPFLAGS@ "${flags[@]}" $libconfig_flags src/*.c
